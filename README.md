# Streamlit-сервис прогноза прибыльности блока

Веб-приложение на Streamlit для прогноза прибыли `Profit (USD)` по геолого-экономическим параметрам блока добычи.

## 🎯 Используемая модель

**Gradient Boosting Regressor** - выбрана как лучшая модель после сравнения с LinearRegression, Ridge и RandomForest по метрикам RMSE и R².

## 📁 Структура проекта

```
├── streamlit_app.py          # Основное приложение Streamlit
├── requirements.txt          # Зависимости Python
├── mining_block_model.csv    # Данные для обучения модели
├── Test_App_1.ipynb          # Ноутбук с EDA и сравнением моделей
├── README.md                 # Документация проекта
└── DEPLOYMENT.md             # Инструкция по развертыванию
```

## 🚀 Локальный запуск

### 1. Установка зависимостей

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Запуск приложения

```bash
streamlit run streamlit_app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`

## ☁️ Развертывание на Streamlit Community Cloud

### Быстрый старт

1. **Создайте репозиторий на GitHub**
   - Зайдите на https://github.com
   - Создайте новый репозиторий (например, `mining-profit-app`)
   - **Не** добавляйте README, .gitignore или лицензию

2. **Загрузите код в репозиторий**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: GradientBoosting model"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   git push -u origin main
   ```

3. **Разверните на Streamlit Cloud**
   - Зайдите на https://share.streamlit.io/
   - Авторизуйтесь через GitHub
   - Нажмите **"New app"**
   - Выберите ваш репозиторий
   - Укажите **Main file path**: `streamlit_app.py`
   - Нажмите **"Deploy"**

4. **Ожидание развертывания**
   - Streamlit Cloud автоматически установит зависимости
   - Обучит модель (займет ~30-60 секунд при первом запуске)
   - Предоставит публичный URL

Подробные инструкции см. в файле `DEPLOYMENT.md`

## 📊 Возможности приложения

- ✅ **Интерактивный прогноз** - введите параметры блока в sidebar и получите прогноз прибыли
- ✅ **Пакетное прогнозирование** - загрузите CSV файл с блоками и получите прогнозы для всех
- ✅ **Метрики качества модели** - отображение RMSE и R² на обучающей и тестовой выборках
- ✅ **Визуализация результатов** - таблица с прогнозами и статистика

## 🔧 Технические детали

### Обработка данных

- Удаление экстремальных выбросов по правилу 3 * IQR
- Масштабирование числовых признаков (StandardScaler)
- One-hot кодирование категориальных признаков
- Разделение на train/test (80/20)

### Модель

- **Алгоритм**: GradientBoostingRegressor
- **Параметры**: n_estimators=100, learning_rate=0.1, max_depth=5
- **Кэширование**: Модель кэшируется через `@st.cache_resource` для ускорения повторных запусков

## 📝 Требования

- Python 3.10+
- Все зависимости указаны в `requirements.txt`

## 🐛 Решение проблем

### Файл не найден
Убедитесь, что `mining_block_model.csv` находится в той же папке, что и `streamlit_app.py`

### Ошибка при обучении модели
Проверьте, что CSV файл содержит все необходимые столбцы:
- `Profit (USD)` (целевая переменная)
- Все признаки из датасета

### Медленная загрузка
Первая загрузка может занять 30-60 секунд из-за обучения модели. Последующие загрузки будут быстрее благодаря кэшированию.

## 📄 Лицензия

Проект создан в образовательных целях.
