# Инструкция по развертыванию сервиса

## 📋 Подготовка к развертыванию

### Проверка файлов

Убедитесь, что в корне проекта есть все необходимые файлы:

```
├── streamlit_app.py          ✅ Основное приложение
├── requirements.txt          ✅ Зависимости
├── mining_block_model.csv    ✅ Данные для обучения
├── README.md                 ✅ Документация
└── DEPLOYMENT.md             ✅ Эта инструкция
```

## 🚀 Развертывание на Streamlit Community Cloud

### Шаг 1: Подготовка Git репозитория

Если репозиторий еще не создан:

```bash
# Инициализация репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: GradientBoosting model for profit prediction"
```

### Шаг 2: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите **"New repository"** (или значок "+" в правом верхнем углу)
3. Заполните форму:
   - **Repository name**: например, `mining-profit-app`
   - **Description**: опционально
   - **Visibility**: Public (для бесплатного Streamlit Cloud) или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (у вас уже есть файлы)
4. Нажмите **"Create repository"**

### Шаг 3: Подключение локального репозитория к GitHub

```bash
# Добавление удаленного репозитория
# Замените YOUR_USERNAME и YOUR_REPO_NAME на ваши значения
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Переименование ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

**Если Git запросит авторизацию:**
- Используйте **Personal Access Token** вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите права: `repo` (полный доступ к репозиториям)
- Скопируйте токен и используйте его как пароль

### Шаг 4: Развертывание на Streamlit Cloud

1. **Зайдите на Streamlit Cloud**
   - Откройте https://share.streamlit.io/
   - Авторизуйтесь через GitHub (используйте ту же учетную запись, где находится репозиторий)

2. **Создайте новое приложение**
   - Нажмите **"New app"**
   - Заполните форму:
     - **Repository**: выберите ваш репозиторий из списка
     - **Branch**: `main` (или другая ветка, если используете)
     - **Main file path**: `streamlit_app.py`
   - Нажмите **"Deploy"**

3. **Ожидание развертывания**
   - Streamlit Cloud автоматически:
     - Установит зависимости из `requirements.txt`
     - Запустит `streamlit_app.py`
     - Обучит модель GradientBoosting (~30-60 секунд при первом запуске)
   - После завершения вы получите **публичный URL** вида:
     `https://YOUR-REPO-NAME.streamlit.app`

### Шаг 5: Проверка работы

1. Откройте URL вашего приложения
2. Убедитесь, что:
   - ✅ Отображаются метрики модели (RMSE, R²)
   - ✅ Можно ввести параметры блока в sidebar
   - ✅ Прогноз прибыли рассчитывается корректно
   - ✅ Пакетное прогнозирование через CSV работает

## 🔄 Обновление уже развернутого сервиса

Если сервис уже был развернут и вы хотите обновить код:

```bash
# Внесите изменения в файлы
# ...

# Добавьте изменения в Git
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте на GitHub
git push
```

Streamlit Cloud автоматически обнаружит изменения и перезапустит приложение (обычно в течение 1-2 минут).

## 🧪 Локальное тестирование перед развертыванием

Перед загрузкой на GitHub рекомендуется протестировать локально:

```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
streamlit run streamlit_app.py
```

**Проверьте:**
- ✅ Модель обучается без ошибок
- ✅ Метрики отображаются корректно
- ✅ Прогноз для одного блока работает
- ✅ Пакетное прогнозирование через CSV работает
- ✅ Нет ошибок в консоли

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'sklearn'"
**Решение**: Убедитесь, что `scikit-learn>=1.5.0` указан в `requirements.txt`

### Проблема: "FileNotFoundError: mining_block_model.csv"
**Решение**: 
- Убедитесь, что файл `mining_block_model.csv` находится в корне репозитория
- Проверьте, что файл закоммичен: `git ls-files | grep mining_block_model.csv`

### Проблема: Модель обучается слишком долго
**Решение**: 
- Это нормально для GradientBoosting при первом запуске (~30-60 сек)
- Последующие запросы будут быстрыми благодаря кэшированию (`@st.cache_resource`)

### Проблема: "Out of memory" на Streamlit Cloud
**Решение**: 
- GradientBoosting использует больше памяти, чем LinearRegression
- Если проблема сохраняется, можно уменьшить параметры модели в `streamlit_app.py`:
  ```python
  model = GradientBoostingRegressor(
      n_estimators=50,  # уменьшить с 100
      max_depth=3,      # уменьшить с 5
      ...
  )
  ```

### Проблема: Ошибка при чтении CSV
**Решение**: 
- Проверьте кодировку файла (должна быть UTF-8)
- Убедитесь, что файл не поврежден

### Проблема: Git не работает в PowerShell
**Решение**: 
- Используйте Git Bash вместо PowerShell
- Или установите Git для Windows и добавьте его в PATH

## 📊 Мониторинг развертывания

После развертывания вы можете:

1. **Просматривать логи**
   - В панели Streamlit Cloud нажмите на ваше приложение
   - Откройте вкладку "Manage app" → "Logs"

2. **Проверять статус**
   - Зеленый индикатор = приложение работает
   - Красный индикатор = есть ошибки (проверьте логи)

3. **Перезапускать приложение**
   - В панели управления нажмите "Reboot app"

## ✅ Чеклист перед развертыванием

- [ ] Все файлы закоммичены в Git
- [ ] `requirements.txt` содержит все необходимые зависимости
- [ ] `mining_block_model.csv` находится в корне проекта
- [ ] Приложение работает локально без ошибок
- [ ] Репозиторий создан на GitHub
- [ ] Код запушен в GitHub
- [ ] Streamlit Cloud подключен к репозиторию

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в панели Streamlit Cloud
2. Убедитесь, что все файлы закоммичены и запушены
3. Проверьте формат файла `mining_block_model.csv`
4. Убедитесь, что `requirements.txt` корректен
